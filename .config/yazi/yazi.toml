[mgr]
show_hidden = true
linemode = "size_mtime"

[[plugin.prepend_fetchers]]
id = "git"
name = "*"
run = "git"

[[plugin.prepend_fetchers]]
id = "git"
name = "*/"
run = "git"

[opener]
extract = [
	{ run = 'ya pub extract --list "$@"', desc = "Extract here", for = "unix" }
]
edit = [
  {run = 'nvim "$@"', block = true, for = "unix"}
]
# edit = [
#   { run= 'NVIM_APPNAME=nvim-minim nvim "$@"', block = true, for = "unix" }
# ]
