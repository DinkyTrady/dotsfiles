(defsrc
  esc caps ; '
) 

(defalias
  capsesc (tap-hold 100 100 caps esc)
  escctrl (tap-hold-release 100 150 esc lctrl)
  ; (tap-hold 100 150 ; rmet)
  ' (tap-hold-release 100 150 ' rctrl )
)

(deflayer base
  @capsesc @escctrl @; @'
)
